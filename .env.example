# Groq API Configuration
# Get your API key from https://console.groq.com/keys
# This is optional - users can provide their own API keys through the UI
GROQ_API_KEY=your_groq_api_key_here

# Email Service (Resend)
# Required for magic link authentication
# Get your API key from https://resend.com/
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=noreply@yourdomain.com

# Security Keys
# Generate a strong random string for JWT signing
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Generate a strong random string for encrypting API keys
# Example: openssl rand -base64 32
ENCRYPTION_KEY=your_encryption_key_here

# Deno KV
# No configuration needed - automatically available in Deno Deploy
# In local development, a local KV database will be created automatically
